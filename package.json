{
  "name": "finance-leasing-dashboard",
  "version": "1.0.0",
  "description": "Finance Leasing Workflow Dashboard with Admin/Viewer Access",
  "type": "module",
  "scripts": {
    "dev": "netlify dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "build:frontend": "cd frontend && npm run build"
  },
  "dependencies": {
    "@netlify/blobs": "^9.0.0",
    "@netlify/functions": "^4.4.0",
    "@netlify/identity": "^1.0.0",
    "csv-parse": "^5.5.6",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
